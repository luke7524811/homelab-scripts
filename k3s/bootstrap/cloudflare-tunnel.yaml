# ...
apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudflared-config
  namespace: cloudflare-tunnel
data:
  # We no longer need the ingress block here.
  # Cloudflare is handling it from the UI.
  config.yaml: ""
# ...
